import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    image: {
        width: 120,
        height:170,
        resizeMode:'cover',
        borderRadius:5,
        margin:5
    },
    title: {
        fontSize:20,
        fontWeight: 'bold',
        color:'white'
    }
});

export default styles;